<?xml version="1.0"?>

Erstmal in scheduler.xml, bis wir die Resourcen auflisten und kopieren können.

<job 
    xmlns:xsi                     = "http://www.w3.org/2001/XMLSchema-instance" 
    xsi:noNamespaceSchemaLocation = "s:/scheduler/engine/kernel-cpp/scheduler/scheduler.xsd"
>
    <script language="shell"><![CDATA[
#! /bin/bash
(sleep 30 && exit 1)&
a=$!
(sleep 30 && exit 1)&
b=$!
sleep 3
kill -15 $a $b
wait $a || exit
wait $b || exit
    ]]></script>
</job>
