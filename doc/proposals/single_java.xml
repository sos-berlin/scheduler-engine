<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet href="../scheduler.xsl" type="text/xsl"?>

<!--$Id$-->

<description
    title               = "Nur eine Java Virtual Machine für alle Java-Tasks"
    base_dir            = "../"
    parent_page         = "index.xml"
    author              = "$Author$"
    date                = "$Date$"
>
    <p>
        Der Scheduler hält einen Prozess für die Java-Tasks vor oder lässt die Java-Tasks im eigenen Prozess des Schedulers laufen.
        Damit lädt der Scheduler nur eine Java Virtual Machine, was den Speicherbedarf und die Startzeit verringert.
        Jede Task läuft unabhängig in einem eigenen Java-Thread.
    </p>
    <p>
        Mit getrennten Classloadern könnten die Tasks weiter voneinander getrennt werden.
    </p>
    <p>
        Das bisherige Verfahren mit eigenen Prozessen bleibt bestehen.
    </p>
    <p>
        Wenn Java im Prozess des Schedulers läuft, kann die Kommunikation zwischen Task und Scheduler beschleunigt werden.
        Statt über TCP oder Pipes werden die Nachrichten im Speicher mit einfacher Signalisierung ausgetauscht.
    </p>
    <pre>&lt;process_class name="one_process" separate_process="no" max_threads="10"/></pre>
    <pre>&lt;job name="my_job" process_class="one_process"></pre>

</description>