<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet href="scheduler.xsl" type="text/xsl"?>

<!--$Id: settings.xml 3616 2005-04-18 21:42:48Z jz $-->

<!-- Einige Einstellungen können an mehreren Stellen vorgenommen werden:
     - Konfigurationsdatei
     - factory.ini
     - Kommandozeile
     - Programm-Schnittstelle

     Diese Einstellungen werden hier beschrieben.
-->

<settings
    base_dir    = ""
    title       = "Settings"
    author      = "$Author: jz $"
    date        = "$Date: 2008-08-14 10:22:14 +0200 (Do, 14 Aug 2008) $"
>

    <setting setting        = "scheduler_log"
             type           = "file_name"
             title          = "scheduler.log file name">
<!--<setting setting        = "scheduler_log"
             type           = "dateiname"
             title          = "Dateiname des Protokols scheduler.log">-->

        <description>
            <p>
                This setting causes the Job Scheduler to write a detailed protocol.
                This protocol is intended for use in problem diagnosis.
                The file name should be fully specified here (i.e. as a full path).
            <!--Wenn diese Einstellung gesetzt ist,
                schreibt der Job Scheduler ein detailliertes Protokoll zur Fehlerdiagnose in die Datei.
                Geben Sie den Dateinamen absolut an (als vollständigen Pfad).-->
            </p>

            <p>
                A plus character (+) written directly before the file name causes an already existing protocol to be continued.
                Otherwise such a protocol will be overwritten.
            <!--Ein Plus (+) direkt vor dem Dateinamen lässt ein vorhandenenes Protokoll fortsetzen.
                Sonst wird es überschrieben.-->
            </p>

            <h3>Categories</h3>
        <!--<h3>Kategorien</h3>-->

            <p>
                Categories can be used to extend or restrict the log file.
                Category names are added (separated by spaces) before the file name,
                which is then preceded by a larger than (>) sign.
            <!--Sie können das Protokoll mit Kategorien erweitern oder einschränken.
                Schreiben Sie die Namen der Kategorien mit Zwischenräumen getrennt vor den Dateinamen,
                den Sie mit einem Größerzeichen (>) einleiten.-->
            </p>

            <p>
                The list of categories can be found <a href="log_categories.xml">here</a>.
            <!--Hier ist die <a href="log_categories.xml">Liste der Kategorien</a>.-->
            </p>
        </description>

        <example><pre>
log = c:/tmp/scheduler.log
log = scheduler.wait >scheduler.log
log = scheduler.wait com_server.* >scheduler.log
        </pre></example>
    </setting>


    <setting setting        = "directory"
             type           = "directory"
             title          = "Working Directory">
<!--<setting setting        = "directory"
             type           = "verzeichnis"
             title          = "Arbeitsverzeichnis">-->

        <description>
            <scheduler_keyword keyword="Working Directory"/>
            <scheduler_keyword keyword="Directory"/>
        <!--<scheduler_keyword keyword="Arbeitsverzeichnis"/>
            <scheduler_keyword keyword="Verzeichnis"/>-->
            <p>
                Changes the Working Directory.
            <!--Ändert das Arbeitsverzeichnis.-->
            </p>
        </description>
    </setting>


    <setting setting        = "log_dir"
             type           = "directory"
             title          = "Protocol directory">
             subst_env      = "yes"
<!--<setting setting        = "log_dir"
             type           = "verzeichnis"
             title          = "Verzeichnis der Protokolle">
             subst_env      = "yes"-->


        <description>
            <p>
                The directory in which the Job Scheduler writes log files.
            <!--In dieses Verzeichnis schreibt der Job Scheduler seine Protokolle.-->
            </p>
            <p>
                <scheduler_setting name="log_dir" value="*stderr"/> allows the Job Scheduler
                to write log files to the standard output (<code>stderr</code>, normally the screen) .
            <!--<scheduler_setting name="log_dir" value="*stderr"/> lässt den Job Scheduler
                die Protokollausgabe nach <code>stderr</code> (auf den Bildschirm) schreiben.-->
            </p>
        </description>
    </setting>


    <setting setting           = "param"
             api               = "Spooler.param"
             type              = "text"
             title             = "For free use">
         <!--title             = "Zur freien Verwendung">-->

        <description>
            <p>
                Free text.
                This parameter can be read using <code>spooler.param</code>.
            <!--Zur freien Verwendung.
                Der Parameter ist in den Skripten über <code>spooler.param</code> lesbar.-->
            </p>
        </description>
    </setting>


    <setting setting= "config"
             type   = "file_name"
             title  = "Configuration file">
         <!--type   = "dateiname"
             title  = "Konfigurationsdatei">-->

        <description>
            <scheduler_keyword keyword="Configuration"/>
        <!--<scheduler_keyword keyword="Konfiguration"/>-->
            <scheduler_keyword keyword="XML Configuration"/>
        <!--<scheduler_keyword keyword="XML-Konfiguration"/>-->
            <p>
                Defines the <a href="konfiguration.xml">Configuration File</a>.
            <!--Gibt die <a href="konfiguration.xml">Konfigurationsdatei</a> an.-->
            </p>
        </description>
    </setting>


    <setting setting= "option_id"
             type   = "scheduler_id"
             title  = "Job Scheduler identifier (id)">
         <!--title  = "Kennung des Job Schedulers">-->

        <description>
            <p>
                The Job Scheduler only selects elements in the XML configuration whose
                <code>spooler_id</code> attributes are either empty or set to the value given here.
            <!--Der Job Scheduler wählt nur die Elemente der XML-Konfiguration aus,
                deren Attribut <code>spooler_id</code> leer ist oder denselben Wert
                wie in dieser Einstellung enthält.-->
            </p>
            <p>
                When the Job Scheduler ID is not specified here, then the Job Scheduler ignores the <code>spooler_id=</code> XML attribute
                and selects all the elements in the XML configuration.
            <!--Wenn die Job Scheduler ID nicht angegeben ist, beachtet der Job Scheduler das XML-Attribut <code>spooler_id=</code>
                nicht und wählt alle Elemente der XML-Konfiguration aus.-->
            </p>
            <p>
                See, for example, <scheduler_element name="config"/>.
            <!--Siehe z.B. <scheduler_element name="config"/>.-->
            </p>
        </description>
    </setting>

    <setting setting= "spooler_id"
             type   = "scheduler_id">

        <description>
            <p>
                An element having this attribute is only active when the attribute is either:
            </p>
            <p>

            <ul>
                <li>
                     empty
                </li>
                <li>
                     set to the <code>-id=</code> Job Scheduler start parameter
                </li>
                <li>
                     or when the Job Scheduler <scheduler_option name="id"/> option is not specified 
                     when starting the Job Scheduler.
                </li>
            </ul>
            </p>
            <!--Das Element ist nur wirksam, wenn dieses Attribut leer oder gleich dem
                Parameter <code>-id=</code> vom Job Scheduler Start ist,
                oder wenn beim Job Scheduler Start die Option <scheduler_option name="id"/> nicht angegeben worden ist.-->
<!--
            <p>
                Gibt die Scheduler-Id an.
                Der Scheduler wählt aus der Konfigurationsdatei die Einträge mit dem Attribut
                <code>spooler_id="</code><i>spooler_id</i><code>"</code> oder
                <code>spooler_id=""</code>
                aus.
            </p>
-->
        </description>
    </setting>


    <setting setting= "port"
             type   = "number"
             initial= "0"
             title  = "HTTP, TCP and UDP ports for control commands for the Job Scheduler">
         <!--title  = "HTTP-, TCP- und UDP-Port für Kommandos an den Job Schedulers">-->

        <description>
            <p>
                    Combines the <code>tcp_port</code> and <code>udp_port</code> settings.
                <!--Fasst die Einstellungen <code>tcp_port</code> und <code>udp_port</code> zusammen.-->
            </p>
        </description>
    </setting>


    <setting setting= "tcp_port"
             type   = "number"
             initial= "0"
             title  = "Port for HTTP and TCP commands for the Job Scheduler">
         <!--title  = "HTTP- und TCP-Port zur Steuerung des Job Schedulers">-->

        <description>
            <p>
                The Job Scheduler can accept commands via a TCP port whilst it is running.
                The number of this port is set here - depending on the operating system - 
                with a number between 2048 and 65535.
                The default value is 4444.
            <!--Der Job Scheduler kann auf einem TCP-Port während des Betriebs Kommandos entgegennehmen. Hier kann ein
                Portnummer eingestellt werden. Die Portnummer ist eine Zahl zwischen  2048 (betriebssystemabhängig) und
                65535. Die voreinstellung lautet 4444.-->
            </p>
            <p>
                The Job Scheduler operates a HTTP/HTML server on the same port, enabling it to be reached using a web browser
                - e.g. via http://localhost:4444.
            <!--Über denselben Port betreibt der Job Scheduler einen HTTP/HTML-Server, so dass er über einen Browser ansprechbar ist,
                z.B.: http://localhost:4444.-->
            </p>
            <p>
                The Job Scheduler does not respond to the <code>tcp_port=0</code> default setting either with TCP or HTTP protocols.
                This setting can therefore be used to block a Job Scheduler from being accessed - for example via TCP.
            <!--Bei <code>tcp_port=0</code> (voreingestellt) ist der Job Scheduler nicht über TCP oder HTTP ansprechbar.
                Damit kann die Steuerung des Job Schedulers per TCP-Kommando, z.B. über eine Web-Seite, unterbunden werden.-->
            </p>
        </description>
    </setting>


    <setting setting= "udp_port"
             type   = "number"
             initial= "0"
             title  = "Port for UDP commands for the Job Scheduler">
         <!--title  = "UDP-Port zur Steuerung des Job Schedulers">-->

        <description>
            <p>
                The Job Scheduler can also accept UDP commands addressed to the port specified in this setting.
                Note that a UDP command must fit in a message and that the Job Scheduler does not answer UDP commands.
            <!--Auch über UDP kann der Job Scheduler Kommandos entgegennehmen.
                Hier wird die Portnummer angegeben.
                Ein Kommando muss in eine Nachricht passen. Der Job Scheduler schickt keine Antwort.-->
            </p>
            <p>
                The default value of <code>udp_port=0</code> does not allow the Job Scheduler to open a UDP port.
            <!--Bei <code>udp_port=0</code> (voreingestellt) öffnet der Job Scheduler keinen UDP-Port.-->
            </p>
        </description>
    </setting>



    <setting setting = "ip_address" 
             type    = "ip_number" 
             initial = "0.0.0.0" 
             title   = "The interface IP address for TCP and UDP">
         <!--title   = "IP-Adresse der Schnittstelle für TCP und UDP">-->
        <description>
            <p>
                The IP address to which the TCP and UDP ports are bound.
                The Job Scheduler can then only be reached by way of this address.
            <!--Die IP-Adresse, an die TCP- und UDP-Port gebunden werden.
                Der Job Scheduler ist dann nur über diese IP-Adresse erreichbar.-->
            </p>
            <p>
                A host name can also be specified.
            <!--Ein Hostname kann angegeben werden.-->
            </p>
            <p>
                The default setting is 0.0.0.0, which stands for all IP addresses.
            <!--Default ist 0.0.0.0, was für alle IP-Adressen steht.-->
            </p>
            <p>
                When another IP address as 127.0.0.1 or localhost is given,
                then the Job Scheduler cannot be reached by way of localhost.
            <!--Wenn eine andere IP-Adresse als 127.0.0.1 oder localhost angegeben ist,
                kann der Job Scheduler nicht über localhost erreicht werden.-->
            </p>
        </description>
    </setting>


    <setting setting= "log_level"
             type   = "log_level"
             initial= "info"
             title  = "Limit protocol level">
         <!--title  = "Protokollausgaben beschränken">-->
        <description>
            <p>
                Defines the level with which protocol entries should be written.
                Every protocol entry is given one of the following categories:
                <code>error</code>, <code>warn</code>, <code>info</code>, <code>debug1</code> to <code>debug9</code>
                (<code>debug1</code> is the same as <code>debug</code>).
            <!--Legt fest, ab welcher Stufe der Scheduler Protokollzeilen ausgibt.
                Jede Protokollzeile ist auf einer der folgenden Stufen:
                <code>error</code>, <code>warn</code>, <code>info</code>, <code>debug1</code> bis <code>debug9</code>
                (<code>debug1</code> ist dasselbe wie <code>debug</code>).-->
            </p>
        </description>
    </setting>


    <setting setting   = "include_path"
             type      = "directory"
             title     = "Directory path for &lt;include&gt;"
             subst_env = "yes">
         <!--type      = "verzeichnis"
             title     = "Verzeichnispfad für &lt;include&gt;"
             subst_env = "yes">-->
        <description>
            <p>
                The directory of the files which are to be included by the <scheduler_element name="include"/> element.
            <!--Verzeichnis der Dateien, die mit <scheduler_element name="include"/> eingezogen werden.-->
            </p>
        </description>
    </setting>


    <setting setting = "html_dir"
             type    = "directory"
             title   = "Directory for HTML files">
             subst_env = "yes">
         <!--type    = "verzeichnis"
             title   = "Verzeichnis der HTML-Dateien">
             subst_env = "yes">-->
        <description>
            <p>
                The directory in which the HTML files for the Job Scheduler HTTP server are to be found.
            <!--In diesem Verzeichnis stehen die HTML-Dateien für den HTTP-Server im Scheduler.-->
            </p>
            <p>
                Should no entry be made here, then the Job Scheduler uses the <code>html</code> directory
                in the configuration file directory.
            <!--Wenn die Angabe fehlt, nimmt der Job Scheduler das Verzeichnis <code>html</code>
                im Verzeichnis der Konfigurationsdatei.-->
            </p>
        </description>
    </setting>


    <setting setting = "db_tasks_table"
             type    = "name"
             initial = "SCHEDULER_TASKS"
             title   = "Name of the task database table">
         <!--title   = "Name der Datenbanktabelle für die Tasks">-->
        <description>
            <p>
                See also <scheduler_method class="Spooler" method="db_tasks_table_name"/>
            <!--Siehe auch <scheduler_method class="Spooler" method="db_tasks_table_name"/>-->
            </p>
        </description>
    </setting>


    <setting setting = "db_history_table"
             type    = "name"
             initial = "SCHEDULER_HISTORY"
             title   = "Name of the history database table">
         <!--title   = "Name der Datenbanktabelle für die Historie">-->
        <description>
            <p>
                See also <scheduler_method class="Spooler" method="db_history_table_name"/>
            <!--Siehe auch <scheduler_method class="Spooler" method="db_history_table_name"/>-->
            </p>
        </description>
    </setting>


    <setting setting = "db_order_history_table"
            type    = "name"
            initial = "SCHEDULER_ORDER_HISTORY"
            title   = "Name of the order history database table">
        <!--title   = "Name der Datenbanktabelle für die Auftragshistorie">-->
        <description>
            <p>
                See also <scheduler_method class="Spooler" method="db_order_history_table_name"/>
            <!--Siehe auch <scheduler_method class="Spooler" method="db_order_history_table_name"/>-->
            </p>
        </description>
    </setting>


    <setting setting = "db_order_step_history_table"
             type    = "name"
             initial = "SCHEDULER_ORDER_STEP_HISTORY"
             title   = "Name of the database table containing the steps in the order history">
         <!--title   = "Name der Datenbanktabelle für die Schritte der Auftragshistorie">-->
    </setting>


    <setting setting = "db_orders_table"
             type    = "name"
             initial = "SCHEDULER_ORDERS"
             title   = "Name of the order database table">
         <!--title   = "Name der Datenbanktabelle für die Aufträge">-->
        <description>
            <p>
                See also <scheduler_method class="Spooler" method="db_orders_table_name"/>
            <!--Siehe auch <scheduler_method class="Spooler" method="db_orders_table_name"/>-->
            </p>
        </description>
    </setting>


    <setting setting = "db_variables_table"
             type    = "name"
             initial = "SCHEDULER_VARIABLES"
             title   = "Name of the Job Scheduler internal variable database table">
         <!--title   = "Name der Datenbanktabelle für die internen Variablen des Job Schedulers">-->
        <description>
            <p>
                The Job Scheduler records internal counters, for example, the ID of the next free task,
                in this database table.
            <!--Der Job Scheduler hält interne Zähler, zum Beispiel für die nächste freie Task ID.-->
            </p>
            <p>
                See also <scheduler_method class="Spooler" method="db_variables_table_name"/>
            <!--Siehe auch <scheduler_method class="Spooler" method="db_variables_table_name"/>-->
            </p>
        </description>
    </setting>


    <setting setting = "db_clusters_table"
             type    = "name"
             initial = "SCHEDULER_CLUSTERS"
             title   = "The name of the cluster database table">
         <!--title   = "Name der Datenbanktabelle für die Cluster">-->
    </setting>


    <setting setting = "history_columns"
             type    = "name, name, ..."
             title   = "Additional columns in the history">
         <!--title   = "Zusätzliche Spalten in der Historie">-->
        <description>
            <p>
                A task using the
                <scheduler_method class="Task" method="set_history_field" java_signature="java.lang.String, java.lang.String"/>
                method can include fields specified here in the database table.
            <!--Eine Task kann mit der Methode
                <scheduler_method class="Task" method="set_history_field" java_signature="java.lang.String, java.lang.String"/>
                ja die hier angegebenen Felder der Datenbanktabelle für die Task-Historie setzen.-->
            </p>
        </description>
    </setting>

    <setting setting = "history"
             type    = "yes|no"
             initial = "yes"
             title   = "Write history?">
         <!--title   = "Historie schreiben?">-->
        <description>
            <p>
                Specifies whether a task history should be written.
                If yes, then the Job Scheduler makes an entry in the database for every task carried out.
                Should the Job Scheduler not be using a database,
                this entry will be made in a file specified in the <scheduler_option name="log-dir"/> directory.
            <!--Gibt ein, ob eine Task-Historie geschrieben werden soll.
                Der Job Scheduler schreibt dann für jede Task einen Satz in die Datenbank,
                oder, wenn der Job Scheduler ohne Datenbank arbeitet, in eine Datei im mit
                <scheduler_option name="log-dir"/> angegebenen Verzeichnis.-->
            </p>
        </description>
    </setting>


    <setting setting = "history_file"
             type    = "file_name"
             title   = "Name of the History File (for operation without a database)">
         <!--type    = "dateiname"
             title   = "Dateiname der Historie (für datenbanklosen Betrieb)">-->
        <description>
            <p>
                Should the parameter <code>history=yes</code> be set and the scheduler not be using a database,
                then it will make an entry in this file for each task carried out.
                If no name is specified for the history file,
                then the Job Scheduler will use its own name.
                Note that the Job Scheduler will only write a history when the
                <scheduler_option name="log-dir"/> option is set.
            <!--Der Job Scheduler nimmt einen eigenen Namen, wenn diese Option fehlt.
                Die Option <scheduler_option name="log-dir"/> muss gesetzt sein - andernfalls schreibt der Job Scheduler die Datei nicht.
                In diese Datei wird, wenn die Option <code>history=yes</code> gesetzt ist und der Job Scheduler keine Datenbank verwendet,
                für jede gelaufende Task ein Satz geschrieben.-->
            </p>
        </description>
    </setting>


    <setting setting= "history_on_process"
             type   = "yes|no|number"
             initial = "0"
             title  = "Write history entry after calling spooler_process()?">
         <!--title  = "Nach spooler_process() Historiensatz schreiben?">-->
        <description>
            <p>
                Should the Job Job Scheduler be writing a history, then the settings <code>history_on_process=no</code> or
                <code>=0</code> cause it to make an entry in the history at the start of a task.
            <!--Bei <code>history_on_process=no</code> oder <code>=0</code> schreibt der Job Scheduler beim Start der Task
                einen Satz in die Historie (wenn eine geführt wird).-->
            </p>
            <p>
                When <code>history_on_process=yes</code> or <code>=1</code> 
                then Job Scheduler only makes an entry after
                <scheduler_method class="Job_impl" method="spooler_process"/> has been called.
                This means that no entry will be made, 
                when <scheduler_method class="Job_impl" method="spooler_open"/>
                returns <code>false</code>.
            <!--Bei <code>history_on_process=yes</code> oder <code>=1</code> schreibt der Job Scheduler erst beim ersten Aufruf
                von <scheduler_method class="Job_impl" method="spooler_process"/> den Satz.
                Damit wird kein Satz geschrieben, wenn <scheduler_method class="Job_impl" method="spooler_open"/>
                <code>false</code> liefert.-->
            </p>
            <p>
                When a number is specified in this setting (<code>history_on_process=x</code>),
                the Job Scheduler makes an entry in the history only after the x-th execution of
                <scheduler_method class="Job_impl" method="spooler_process"/>.
            <!--Wenn eine Zahl angegeben ist, schreibt der Job Scheduler erst vor dem
                angegebenen Anzahl von Aufrufen von <scheduler_method class="Job_impl" method="spooler_process"/> einen Satz in die Historie.-->
            </p>
        </description>
        
        <messages>
            <message code="SCHEDULER-335" level="error"/>
        </messages>   
    </setting>


    <setting setting = "history_archive"
             type    = "yes|no|gzip"
             initial = "no"
             title   = "Archive history files?">
         <!--title   = "Historiendateien archivieren?">-->
        <description>
            <p>
                This option causes the Job Scheduler to archive and compress the history files with <code>gzip</code> after its last run,
                should the Job Scheduler be writing the history in files and not in a database.
            <!--Wenn die Historie in einer Datei d.h. nicht in einer Datenbank geführt wird,
                kann der Job Scheduler die Historien des letzten Job Scheduler Laufs archivieren und mit <code>gzip</code> komprimieren.-->
            </p>
        </description>
    </setting>


    <setting setting = "history_with_log"
             type    = "yes|no|gzip"
             initial = "no"
             title   = "Write a protocol in the history?">
         <!--title   = "Protokoll in die Historie schreiben?">-->
        <description>
            <p>
                The Job Scheduler can add the task protocol to the history when the history is being recorded in a database,
                if required, compressed with gzip.
                (Here it is important to check that the protocol - which is saved in binary format -
                can be decompressed after being saved in the database.)
            <!--Der Job Scheduler kann auch das Task-Protokoll in die Historie in der Datenbank schreiben,
                auch mit <code>gzip</code> komprimiert.-->
                <br/>
            </p>
            <p>
                See the command:<scheduler_element name="show_task" attribute="what" value="log"/>.
            <!--Siehe Kommando <scheduler_element name="show_task" attribute="what" value="log"/>.-->
            </p>
        </description>
    </setting>

    <setting setting = "order_history"
             type    = "yes_no"
             initial = "yes"
             title   = "Write orders in the history?">
         <!--title   = "Aufträge in die Historie schreiben?">-->
        <description>
            <p>
                A separate history is recorded for orders.
            <!--Für die Aufträge gibt es eine eigene Historie.-->
            </p>
        </description>
    </setting>


    <setting setting = "order_history_with_log"
             type    = "yes|no|gzip"
             initial = "no"
             title   = "Record the order log in the history?">
         <!--title   = "Auftragsprotokolle in die Historie schreiben?">-->
        <description>
            <p>
                The Job Scheduler can record the order log in the database - compressed with gzip if required.
                (Here it is important to check that the log - which is saved in binary format -
                can be decompressed after being saved in the database.)
            <!--Der Job Scheduler kann das Auftragsprotokoll in die Datenbank schreiben,
                auch mit gzip komprimiert.-->
            </p>
            <p>
                See the command:<scheduler_element name="show_order" attribute="what" value="log"/>.
            <!--Siehe Kommando <scheduler_element name="show_order" attribute="what" value="log"/>.-->
            </p>
        </description>
    </setting>


    <setting setting = "db"
             type    = "connection_string"
             title   = "Database connection string">
         <!--type    = "verbindungszeichenfolge"
             title   = "Verbindungszeichenfolge der Datenbank">-->
        <description>
            <p>
                The database connection string for the history. Should no value be specified here,
                then the files will be saved in .csv format.
                See <scheduler_ini_entry file="factory.ini" section="spooler" entry="history_file"/>.
            <!--Hostware-Dateiname der Datenbank für die Historienführung.
                Ohne Eintrag werden Dateien im .csv-Format erzeugt, siehe
                <scheduler_ini_entry file="factory.ini" section="spooler" entry="history_file"/>.-->
            </p>
            <p>
                A simple file name ending in <code>.mdb</code> (e.g. <code>scheduler.mdb</code>)
                can also be specified here when the Job Scheduler is running on Windows.
                The Job Scheduler then uses a Microsoft MS Access database of this name,
                which is located in the protocol directory (see the option <scheduler_option name="log-dir"/>).
                Should such a database not exist, then the Job Scheduler will create this database.
            <!--Unter Windows kann ein einfacher Dateiname angegebenen werden,
                der auf <code>.mdb</code> endet (z.B. <code>scheduler.mdb</code>).
                Der Job Scheduler verwendet dann eine Microsoft MS-Access Datenbank dieses Namens,
                die im Protokollverzeichnis (s. Option <scheduler_option name="log-dir"/>) liegt.
                Der Job Scheduler richtet die Datei ein, wenn sie fehlt.-->
            </p>
            <p>
                The Job Scheduler automatically creates the tables necessary for this database.
            <!--Der Job Scheduler richtet die Tabllen selbständig ein.-->
            </p>
        </description>

        <example>

<pre>
;    SQL Server 2000 via msbase.jar, msutil.jar, mssqlserver.jar
db = jdbc -class=com.microsoft.jdbc.sqlserver.SQLServerDriver jdbc:microsoft:sqlserver://localhost:1433;selectMethod=Cursor;databaseName=scheduler -user=scheduler -password=secret
;    SQL Server 2000, 2005 via sqljdbc.jar
db = jdbc -class=com.microsoft.sqlserver.jdbc.SQLServerDriver jdbc:sqlserver://localhost:1433;sendStringParametersAsUnicode=false;selectMethod=cursor;databaseName=scheduler -user=scheduler -password=secret

;    MySQL 4.1.7, 5.x
db = jdbc -class=com.mysql.jdbc.Driver jdbc:mysql://localhost/scheduler:3306 -user=scheduler -password=secret

;    Oracle 8.1.7, 9i, 10g
db = jdbc -class=oracle.jdbc.driver.OracleDriver jdbc:oracle:thin:@localhost:1521:orcl -user=scheduler -password=secret

;    PostgreSQL 8.x
db   = jdbc -class=org.postgresql.Driver jdbc:postgresql://localhost:5432/scheduler -user=scheduler -password=secret

;    IBM DB2 8
db   = jdbc -class=com.ibm.db2.jcc.DB2Driver jdbc:db2://localhost:50000/scheduler:driverType=2;retrieveMessagesFromServerOnGetMessage=true; -user=scheduler -password=secret

;    Firebird 1.5
db   = jdbc -class=org.firebirdsql.jdbc.FBDriver jdbc:firebirdsql://localhost:3050/scheduler -user=scheduler -password=secret

;    ODBC
db = odbc -db=scheduler_datasource -user=scheduler -password=secret

;    MS Access Datenbank
db = scheduler.mdb
</pre>

        </example>
    </setting>


    <setting setting = "need_db"
             type    = "yes|no|strict"
             initial = "no"
             title   = "Is a database necessary?">
         <!--title   = "Ist die Datenbank notwendig?">-->
        <description>
            <dl>
                <dt><code>need_db=no</code></dt>
                <dd>
                    <p>
                        Should the option <scheduler_setting setting="db"/> not have been set,
                        then the Job Scheduler issues a warning and operates without a database.
                    <!--Wenn die Einstellung <scheduler_setting setting="db"/> fehlt,
                        gibt der Job Scheduler eine Warnung aus und arbeitet ohne Datenbank.-->
                    </p>
                    <p>
                        The Job Scheduler also issues a warning and operates without a database,
                        should either the opening of a database or the automatic creation of the necessary tables
                        not be successful.
                    <!--Wenn das Öffnen der Datenbank oder das automatische Anlegen der Tablen fehlschlägt,
                        gibt der Job Scheduler eine Warnung aus und arbeitet ohne Datenbank.-->
                    </p>
                    <p>
                        In the event of a database error when the Job Scheduler is in operation, the Job Scheduler closes and
                        reopens the database.
                        Should the Job Scheduler be unsuccessful in either of these operations,
                        then it will operate without the database.
                    <!--Bei einem Datenbankfehler während des Betriebs schließt der Job Scheduler die Datenbank und
                        öffnet sie erneut.
                        Wenn das fehlschlägt, arbeitet der Job Scheduler ohne Datenbank weiter.-->
                    </p>
                    <p>
                        In Cluster operation (<scheduler_option name="exclusive"/> or <scheduler_option name="distributed-orders"/>)
                        the Job Scheduler rejects <code>need_db=no</code> with the following message:
                        <scheduler_message code="SCHEDULER-358" show_text="yes"/>
                    <!--Im Cluster-Betrieb (<scheduler_option name="exclusive"/> oder <scheduler_option name="distributed-orders"/>)
                        lehnt der Job Scheduler <code>need_db=no</code> ab mit der Meldung:
                        <scheduler_message code="SCHEDULER-358" show_text="yes"/>-->
                    </p>
                </dd>

                <dt><code>need_db=yes</code></dt>
                <dd>
                    <p>
                        In this case, in the event of the <scheduler_setting setting="db"/> setting not being specified,
                        then the Job Scheduler will not start but return the following message,
                        <scheduler_error code="SCHEDULER-205"/>.
                    <!--Wenn die Einstellung <scheduler_setting setting="db"/> fehlt,
                        startet der Job Scheduler nicht und gibt die Meldung
                        <scheduler_error code="SCHEDULER-205"/> aus.-->
                    </p>
                    <p>
                        The Job Scheduler will not start should either the opening of the database or
                        the automatic creation of tables not function successfully.
                    <!--Wenn das Öffnen der Datenbank oder das automatische Anlegen der Tablen fehlschlägt,
                        startet der Job Scheduler nicht.-->
                    </p>
                    <p>
                        The Job Scheduler attempts to close and then reopen the database should a database error
                        occur whilst it is in operation.
                        Should the Job Scheduler not be successful in these operations,
                        then it will reattempt to open the database at one minute intervals.
                        It will continue with these attempts to reopen the database indefinitely.
                        The Job Scheduler will not attempt to manage any tasks until it regains contact with the database.
                    <!--Bei einem Datenbankfehler während des Betriebs schließt der Job Scheduler die Datenbank und
                        öffnet sie erneut.
                        Wenn das fehlschlägt, versucht der Job Scheduler endlos im Abstand von einer Minute
                        die Datenbank erneut zu öffnen.
                        Die Tasks werden währenddessen nicht bedient.-->
                    </p>
                    <p>
                        The Job Scheduler sends an <a href="email.xml">E-mail</a> should repeated attempts to
                        reopen the database be unsuccessful.
                    <!--Wenn sich die Datenbank nicht erneut öffnen lässt,
                        schickt er eine <a href="email.xml">eMail</a>.-->
                    </p>
                </dd>

                <dt><code>need_db=strict</code></dt>
                <dd>
                    <p>
                        As <code>need_db=yes</code> with the following exceptions:
                    <!--Wie <code>need_db=yes</code> mit folgender Ausnahme.-->
                    </p>
                    <p>
                        Should a database error occur whilst the Job Scheduler is in operation,
                        then it attempts to close and then reopen the database.
                        Should an error repeat itself then the Job Scheduler will repeat this process
                        for the number of times specified in the <scheduler_setting setting="max_db_errors"/> setting.
                        Should the scheduler not be successful reopening the database, then it will shut itself down.
                    <!--Bei einem Datenbankfehler während des Betriebs schließt der Job Scheduler die Datenbank und
                        öffnet sie erneut.
                        Bei einem erneuten Fehler wiederholt der Job Scheduler den Versuch so oft,
                        wie mit <scheduler_setting setting="max_db_errors"/> eingestellt.
                        Wenn das fehlschlägt, beendet sich der Job Scheduler.-->
                    </p>
                </dd>
            </dl>
        </description>
    </setting>


    <setting setting = "max_db_errors"
             type    = "number"
             initial = "5"
             title   = "Number of database errors before the Job Scheduler shuts itself down">
         <!--type    = "zahl"
             initial = "5"
             title   = "Anzahl Datenbank-Fehler, bevor Job Scheduler aufgibt">-->
        <description>
            <p>
                The Job Scheduler accepts this number of database errors.
                In the event of this number of errors being exceeded, and the option <code>need_db=yes</code>,
                then the scheduler shuts itself down immediately.
                Should <code>need_db=no</code> then the Job Scheduler will continue without the database.
            <!--Der Job Scheduler toleriert diese Anzahl Datenbankfehler.
                Wird sie überschritten, dann setzt er bei <code>need_db=no</code> ohne Datenbank fort,
                anderfalls beendet er sich sofort.-->
            </p>
            <p>
                In the case of <scheduler_setting setting="need_db" value="yes"/>,
                then the errors occurring when attempting to open a database will not be not counted.
                This means that the Job Scheduler can wait for a database which has not yet been opened.
            <!--Bei <scheduler_setting setting="need_db" value="yes"/> werden Fehler,
                die beim Versuch, die Datenbank zu öffnen, auftreten, nicht gezählt.
                Damit kann er Job Scheduler auf eine Datenbank warten, die noch nicht angelaufen ist.-->
            </p>
        </description>
    </setting>

<!-- setting depreciated:
    <setting setting = "mail_encoding"
             type    = "quoted-printable|base64"
             initial = "quoted-printable"
             title   = "E-mail attachment coding">-->
         <!--title   = "Codierung des eMail-Anhangs">-->
<!--    <description>
            <p>
                The protocol is coded in quoted-printable before sending.
                This coding can be changed in the <code>factory.ini</code> configuration file to another system
                compatible with Jmail (from Dimac).-->
            <!--Das Protokoll wird beim Versand mit quoted-printable codiert. Diese Codierung
                kann in der Konfigurationsdatei <code>factory.ini</code> auf einen anderen von Jmail akzeptierten Wert geändert
                werden (für JMail von Dimac).-->
<!--        </p>
        </description>
    </setting>-->


    <setting setting = "mail_on_error"
             type    = "yes|no"
             initial = "no"
             title   = "Send an e-mail should a task end with an error">
         <!--title   = "Mit Fehler beendete Task verschickt eMail">-->
    </setting>


    <setting setting = "mail_on_warning"
             type    = "yes|no"
             initial = "no"
             title   = "An e-mail is sent after a task has ended, should a warning or an error occur">
         <!--title   = "Wenn eine Warnung oder ein Fehler ausgegeben worden ist, wird am Task-Ende eine eMail verschickt">-->
    </setting>


    <setting setting = "mail_on_process"
             type    = "yes|no|number"
             initial = "0"
             title   = "Send an e-mail at the start of a task using spooler_process()">
         <!--title   = "Task mit spooler_process() verschickt eMail">-->
        <description>
            <p>
                Causes the task log to be sent 
                when a task has completed at least the specified number of steps 
                - i.e. calls of <scheduler_method class="Job_impl" method="spooler_process"/>.
                Because non-API tasks do not have steps, the Job Scheduler counts each task as a single step.<!-- ??Ist korrekt? -->
            <!--Veranlasst den Versand des Task-Protokolls, 
                wenn die Task wenigstens die angegebene Anzahl Schritte,
                also Aufrufe von <scheduler_method class="Job_impl" method="spooler_process"/>,
                ausgeführt hat.
                Weil Nicht-API-Tasks keine Schritte haben, zählt der Scheduler für sie immer einen Schritt.-->
            </p>
        </description>
    </setting>


    <setting setting = "mail_on_success"
             type    = "yes|no"
             initial = "no"
             title   = "Send an e-mail on successful completion of a task">
         <!--title   = "Erfolgreich gelaufende Task verschickt eMail">-->
    </setting>


    <setting setting = "mail_on_delay_after_error"
             type    = ""
             initial = "first_and_last_only"
             title   = "Suppress &lt;delay_after_error>">
         <!--title   = "Unterdrückung der eMail bei &lt;delay_after_error>">-->
        <description>
            <p>
                Prerequisite: <code>mail_on_error=yes</code> or <code>mail_on_warning=yes</code>
            <!--Voraussetzung: <code>mail_on_error=yes</code> oder <code>mail_on_warning=yes</code>-->
        </p>
            <p>
                This setting reduces the numerous e-mails after a job is restarted by way of 
                <scheduler_element name="delay_after_error"/>.
            <!--Wenn ein Job wegen <scheduler_element name="delay_after_error"/> erneut startet,
                werden mit dieser Einstellungen die zahlreichen eMails reduziert.-->
            </p>
            
            <dl>
                <dt>
                    <code>mail_on_delay_after_error=all</code>
                </dt>
                <dd>
                    An e-mail is sent after an error occurs in a job.
                <!--Nach jedem fehlerhaften Job-Lauf wird eine eMail verschickt.-->
                </dd>

                <dt>
                    <code>mail_on_delay_after_error=first_only</code>
                </dt>
                <dd>
                    Only the first occurrence of an error causes an e-mail to be sent - 
                    mails are not sent on further occurrences of the same error.
                <!--Nur der erste fehlerhafte Lauf veranlasst eine eMail, nicht aber die Wiederholungen.-->
                </dd>

                <dt>
                    <code>mail_on_delay_after_error=last_only</code>
                </dt>
                <dd>
                    Only the first occurrence of an error causes an e-mail to be sent.
                <!--Nur der letzte fehlerhafte Lauf, der also den Job stoppen lässt, veranlasst eine eMail.-->
                </dd>

                <dt>
                    <code>mail_on_delay_after_error=first_and_last_only</code>
                </dt>
                <dd>
                    Only the first and last occurrences of an error cause an e-mail to be sent.
                <!--Nur der erste und der letzte fehlerhafte Lauf veranlassen je eine eMail.-->
                </dd>
            </dl>
            
            <p>
                This setting only works when <scheduler_element name="delay_after_error"/>
                has been set for as job. 
                Should this not be the case, then <code>mail_on_delay_after_error=all</code> applies.
            <!--Die Einstellung wirkt nicht, wenn für den Job kein <scheduler_element name="delay_after_error"/>
                angegeben ist (dann gilt <code>mail_on_delay_after_error=all</code>).-->
            </p>
        </description>
    </setting>


    <setting setting = "mail_queue_dir"
             type    = "directory"
             title   = "Directory for e-mails which temporarily cannot be sent"
             subst_env = "yes">
         <!--type    = "verzeichnis"
             title   = "Verzeichnis der zeitweilig nicht versendbaren eMails"
             subst_env = "yes">-->
        <description>
            <p>
                E-mails which cannot be sent (because, for example, the SMTP server cannot be contacted)
                are stored in this directory.
            <!--eMails, die der Job Scheduler nicht verschicken kann (weil z.B. der SMTP-Server nicht erreichbar ist),
                werden hier abgelegt.-->
            </p>
            <p>
                In order to send these e-mails later it is necessary to write a job which calls up the
                <scheduler_method class="Mail" method="dequeue"/> method.
            <!--Um diese eMails später zu verschicken, muss ein Job geschrieben werden, der die
                Methode <scheduler_method class="Mail" method="dequeue"/> aufruft.-->
            </p>
            <p>
                This setting is generally made in 
            <!--Der allgemeine Platz für diese Einstellung ist -->
                <scheduler_ini_entry file="sos.ini" section="mail" entry="queue_dir"/>.
            </p>
        </description>
    </setting>


    <setting setting = "mail_queue_only"
             type    = "yes|no"
             title   = "Do not send e-mail, add it to the e-mail queue">
         <!--title   = "E-Mail nicht versenden, nur in die eMail-Warteschlange hängen">-->
        <description>
            <p>
                When this parameter is set to <code>yes</code>, then e-mails are sot sent but
                added to the e-mail queue - 
                see <scheduler_ini_entry file="sos.ini" section="mail" entry="queue_dir"/>.
            <!--Wenn <code>yes</code>, dann werden eMails nicht versendet,
                sondern in eMail-Warteschlange geschrieben,
                siehe <scheduler_ini_entry file="sos.ini" section="mail" entry="queue_dir"/>.-->
            </p>
            <p>
                This setting is generally made in 
            <!--Der allgemeine Platz für diese Einstellung ist -->
                <scheduler_ini_entry file="sos.ini" section="mail" entry="queue_only"/>.
            </p>
        </description>
    </setting>


    <setting setting = "mail_xslt_stylesheet"
             type    = "path"
             title   = "The path to the XSLT style sheet used for the preparation of e-mails">
         <!--title   = "Pfad des XSLT-Stylesheets zur eMail-Aufbereitung">-->
        <description>
            <p>
                The path to the XSLT style sheet.
                XSLT style sheets are used by the Job Scheduler for the preparation of e-mails.
                At the time of writing (April 2006) this subject is not documented.
            <!--Der Pfad eines XSLT-Stylesheets.
                Der Job Scheduler verwendet das XSLT-Stylesheet zum Aufbereiten der eMails.
                (Die Dokumentation hierzu ist noch nicht geschrieben.)-->
            </p>
        </description>
    </setting>


    <setting setting   = "java_class_path"
             type      = "file_names"
             prepend   = "yes"
             subst_env = "yes"
             title     = "Java class path">
         <!--type      = "dateinamen"
             prepend   = "yes"
             subst_env = "yes"
             title     = "Java-Classpath">-->
        <example>
            <pre>class_path = s:\prod\bind\sos.*.jar;c:\jar\my.jar;c:\jar\*</pre>
        </example>
        <description>
            <p>
                The Java <code>CLASS_PATH</code> setting is made here.
                This is a list of paths - on Windows Systems these paths are separated by semi-colons (;),
                on Unix systems by colons (:).
            <!--Für Java kann hier der <code>CLASS_PATH</code> eingestellt werden.
                Das ist eine Liste von Pfaden, in Windows durch Semikolons (;), in Unix
                durch Doppelpunkte getrennt.-->
            </p>
            <p>
                Note that jokers can be used in these paths.
                The Job Scheduler then replaces these jokers with the respective file names.
                i.e. those existing in the file system.
            <!--In den einzelnen Pfaden können Joker benutzt werden.  
                Der Job Scheduler ersetzt diese Pfade durch die tatsächlichen, im Dateisystem vorhandenen.-->
            </p>
            <ul>
                <li>
                    On Windows systems, the <code>*</code> and <code>?</code> characters may be used after the last directory separator.
                <!--Unter Windows sind hinter dem letzten Verzeichnisstrich die Zeichen <code>*</code> und <code>?</code> möglich.-->
                </li>
                <li>
                    On Unix systems the <code>[</code> and <code>]</code> characters can also be 
                    used.
                    Jokers can be used in every folder name of a path
                    (as in the <code>csh</code> shell).
                <!--Unter Unix außerdem die Zeichen <code>[</code> und <code>]</code>;
                    die Joker können auch in jedem Verzeichnis des Pfads auftreten
                    (wie in der Shell <code>csh</code>).-->
                </li>
                <li>
                    Linux (GNU) also recognizes the <code>{,}</code> characters, 
                    when used in the following syntax:<br/>
                    "<i>xxx</i><code>{</code><i>alternative1</i><code>,</code><i>alternative2</i><code>,</code>...<code>}</code><i>xxx</i>",
                    e.g. <code>/dir/sos.{hostware,mail.spooler}.jar</code>.
                <!--Linux (Gnu) kennt außerdem die Zeichen <code>{,}</code> in der Syntax 
                    "<i>xxx</i><code>{</code><i>alternative1</i><code>,</code><i>alternative2</i><code>,</code>...<code>}</code><i>xxx</i>",
                    z.B. <code>/dir/sos.{hostware,mail.spooler}.jar</code>.-->
                </li>
            </ul>
            <p>
                The following points apply to every path in the <code>class_path</code>:
            <!--Dabei gilt für jeden Pfad in <code>class_path</code>:-->
            </p>
            <ul>
                <li>
                    A path without a joker will be handed over to Java unchanged.
                <!--Ein Pfad ohne Joker wird unverändert an Java übergeben.-->
                </li>
                <li>
                    A path containing a joker will be converted to the existing path before being handed over.
                <!--Ein Pfad mit Joker wird in die tatsächlichen, im Dateisystem vorhandenen Pfade aufgelöst.-->
                </li>
                <li>
                    Should no path corresponding to a joker be found then the path will be ignored.
                <!--Wenn auf den Pfad mit Joker kein tatsächlicher Pfad passt, 
                    wird er ignoriert.-->
                </li>
                <li>
                    A path will also be ignored should it refer to a directory which cannot be read.
                <!--Wenn ein Pfad mit ein nicht lesbares Verzeichnis bezeichnet, 
                    wird es ignoriert.-->
                </li>
                <!--li>
                    Unter Linux sind die generierenden Zeichen <code>{</code> und <code>}</code> möglich.
                    Sie erzeugen Pfade, unabhängig davon, ob es sie wirklich gibt.
                    Beispiel: 
                    "<code>/dir/sos.{hostware,mail.spooler}.jar</code>"
                    wird zu
                    "<code>/dir/sos.hostware.jar:/dir/sos.mail.jar:/dir/sos.spooler.jar</code>".
                </li-->
                <li>
                    Environment variables (e.g. <code>${HOME}</code>) will be replaced 
                    before a path is converted.
                <!--Umgebungsvariablen (z.B. <code>${HOME}</code>) werden vorher ersetzt.-->
                </li>
            </ul>
            <p>
                These settings are generally made in the
                <scheduler_ini_entry file="sos.ini" section="java" entry="class_path"/> file.
            <!--Der allgemeine Platz für diese Einstellung ist
                <scheduler_ini_entry file="sos.ini" section="java" entry="class_path"/>.-->
            </p>
        </description>
    </setting>


    <setting setting   = "java_options"
             type      = "text"
             prepend   = "yes"
             subst_env = "yes"
             title     = "Java options">
         <!--title     = "Java-Optionen">-->
        <description>
            <p>
                This setting specifies the directory in which the HostJava is installed -
                e.g. <code>-Djava.library.path=</code>.
            <!--Hier können Sie z.B. mit <code>-Djava.library.path=</code> das Verzeichnis angeben,
                in dem HostJava installiert ist.-->
            </p>
            <p>
                These options are passed to the Java Virtual Machine.
            <!--Die Optionen werden der Java Virtual Machine übergeben.-->
            </p>
            <p>
                These settings are generally made using
                <scheduler_ini_entry file="sos.ini" section="java" entry="options"/>.
            <!--Der allgemeine Platz für diese Einstellung ist
                <scheduler_ini_entry file="sos.ini" section="java" entry="options"/>.-->
            </p>
        </description>
        <example>
            <pre>options = -Xdebug -Djava.library.path=s:/prod/bind</pre>
        </example>
    </setting>


    <setting setting = "javac"
             type    = "file_name"
             initial = "javac"
             title   = "File name for the Java compiler">
         <!--title   = "Dateiname des Java-Compilers">-->
        <description>
            <p>
            </p>
        </description>
    </setting>


    <setting setting = "log_mail_subject"
             type    = "text"
             title   = "E-mail subject">
         <!--title   = "Betreff der eMails">-->
        <description>
            <p>
            </p>
        </description>
    </setting>


    <setting setting = "log_mail_from"
             type    = "email_address"
             title   = "E-mail sender">
         <!--title   = "Absender der eMails">-->
        <description>
            <p>
            </p>
        </description>
    </setting>


    <setting setting = "log_mail_to"
             type    = "email_address"
             title   = "E-mail recipient">
         <!--title   = "To-Empfänger der eMails">-->
        <description>
            <p>
            </p>
        </description>
    </setting>


    <setting setting = "log_mail_cc"
             type    = "email_address"
             title   = "E-mail cc recipient">
         <!--title   = "Cc-Empfänger der eMails">-->
        <description>
            <p>
            </p>
        </description>
    </setting>


    <setting setting = "log_mail_bcc"
             type    = "email_address"
             title   = "E-mail bcc recipient">
         <!--title   = "Bcc-Empfänger der eMails">-->
        <description>
            <p>
            </p>
        </description>
    </setting>


    <setting setting = "smtp"
             type    = "host_address"
             title   = "E-mail SMPT server hostname or IP number">
         <!--title   = "Hostname oder IP-Nummer des SMTP-Servers für eMail versand">-->
        <description>
            <p>
                These settings are generally made using
                <scheduler_ini_entry file="sos.ini" section="mail" entry="smtp"/>.
            <!--Der allgemeine Platz für diese Einstellung ist
                <scheduler_ini_entry file="sos.ini" section="mail" entry="smtp"/>.-->
            </p>
            <p>
                <code>smtp=-queue</code> stops e-mails being sent.
                Instead mails are written into the file specified in 
                <code>queue_dir</code>.
                See also <scheduler_ini_entry file="sos.ini" section="mail" entry="queue_only"/>.
            <!--<code>smtp=-queue</code> verhindert den eMail-Versand und
                schreibt die Nachrichten stattdessen in die mit
                <code>queue_dir</code> eingestellte Datei.
                Siehe auch <scheduler_ini_entry file="sos.ini" section="mail" entry="queue_only"/>.-->
            </p>
        </description>
    </setting>

    <setting setting = "subprocess.own_process_group"
             type    = "yes|no"
             title   = "Start a Sub-Process in its own Group"
             initial = "no">
         <!--title   = "Subprozesse in eigener Prozessgruppe starten"-->
        <description>
            <p>
                This is the default setting for 
            <!--Voreinstellung für -->
                <scheduler_method class="Subprocess" property="own_process_group"/>
            </p>
        </description>
    </setting>

    <setting setting = "tmp"
             type    = "path"
             title   = "Directory for Temporary Files">
         <!--title   = "Verzeichnis der temporären Dateien">-->
        <description>
            <p>
                The default setting is the <code>TMP</code> environment variable.<!-- KURZE-ÜBERSETZUNG MIT JZ ABGESPROCHEN-->
            <!--Voreinstellung ist die Umgebungsvariable <code>TMP</code>,
                unter Windows auch <code>TEMP</code>.-->
            </p>
            <p>
                On Unix systems <code>/tmp/</code> is used, should the <code>TMP</code> environment variable
                not have been set.
            <!--Unter Unix wird <code>/tmp/</code> verwendet, wenn die Umgebungsvariable nicht gesetzt ist.-->
            </p>
        </description>
    </setting>


    <setting setting = "configuration_directory"
             type    = "path"
             title   = "Path to the Configuration Directory">
         <!--title   = "Pfad des Konfigurationsverzeichnisses">-->
    </setting>

<!--
    <setting setting = "run_time_begin"
             type    = "hh:mm[:ss]"
             initial = "00:00">
        <description>
            <p>
                Wenn das Element leer ist (also kein <code>&lt;period></code> enthält),
                generiert der Job Scheduler eine Periode mit dieser Einstellung,
                anderfalls ist es die Voreinstellung für die Kind-Elemente.
            </p>
        </description>
    </setting>


    <setting setting = "run_time_end"
             type    = "hh:mm[:ss]"
             initial = "24:00"
             title   = "">
        <description>
            <p>
                Wenn das Element leer ist (also kein <code>&lt;period></code> enthält),
                generiert der Job Scheduler eine Periode mit dieser Einstellung,
                anderfalls ist es die Voreinstellung für die Kind-Elemente.
            </p>
        </description>
    </setting>


    <setting setting = "run_time_single_start"
             type    = "hh:mm[:ss]"
             initial = ""
             title   = "">
        <description>
            <p>
                Wenn das Element leer ist (also kein <code>&lt;period></code> enthält),
                generiert der Job Scheduler eine Periode mit dieser Einstellung.
            </p>
        </description>
    </setting>


    <setting setting = "run_time_let_run"
             type    = "yes_no"
             initial = ""
             title   = "">
        <description>
            <p>
                Voreinstellung für <scheduler_element name="period"/>.
            </p>
        </description>
    </setting>
-->

    <description>
        <scheduler_keyword keyword="Settings"/>
    <!--<scheduler_keyword keyword="Einstellungen"/>-->
        <p>
            The following settings can be made at different locations:
        <!--Hier sind Einstellungen aufgeführt, die an mehreren Stellen vorgenommen werden können.-->
        </p>
    </description>

</settings>
