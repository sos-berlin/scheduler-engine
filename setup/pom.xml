<?xml version="1.0" encoding="utf-8"?>
<!--
call prepare-setup.sh <version> to use this pom
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.sos.scheduler.engine</groupId>
        <artifactId>engine</artifactId>
        <version>1.5-SNAPSHOT</version>
    </parent>

    <properties>
        <setup.platform>${osArch}</setup.platform>
    </properties>

    <artifactId>setup</artifactId>
    <packaging>pom</packaging>
    <name>${project.artifactId}</name>

    <profiles>
        <!-- this profile is for preparing the setup for a given version on a given platform, e.g.
              mvn install -Pprepare-platform -Dengine.platform=windows-x86
        -->
        <profile>
            <id>prepare-platform</id>
            <properties>
                <setup.platform>${engine.platform}</setup.platform>
            </properties>
        </profile>
    </profiles>

    <repositories>
        <repository>
            <id>sos</id>
            <name>SOS Internal Repository</name>
            <url>http://archiva.sos:8080/archiva/repository/sos</url>
            <snapshots>
                <enabled>true</enabled>
                <!-- updatePolicy>always</updatePolicy -->
            </snapshots>
        </repository>
        <repository>
            <id>sos-os</id>
            <name>SOS snapshot Repository (${setup.platform})</name>
            <url>http://archiva.sos:8080/archiva/repository/sos-os-${setup.platform}</url>
            <snapshots>
                <enabled>true</enabled>
                <!-- updatePolicy>always</updatePolicy -->
            </snapshots>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy</id>
                        <phase>install</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <!-- fat jar mit allen benötigten jars der JobScheduler engine (derzeit nicht ausgeliefert) -->
                                <artifactItem>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>jar</artifactId>
                                    <version>${project.version}</version>
                                    <type>jar</type>
                                    <destFileName>${project.groupId}-${project.version}.jar</destFileName>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>com.sos.scheduler.enginedoc</groupId>
                                    <artifactId>enginedoc-kernel</artifactId>
                                    <version>${enginedoc.version}</version>
                                    <type>jar</type>
                                    <destFileName>com.sos.scheduler.enginedoc-kernel-${enginedoc.version}.jar</destFileName>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>com.sos.scheduler.enginedoc</groupId>
                                    <artifactId>enginedoc-common</artifactId>
                                    <version>${enginedoc.version}</version>
                                    <type>jar</type>
                                    <destFileName>com.sos.scheduler.enginedoc-common-${enginedoc.version}.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <configuration>
                    <target>
                        <ant antfile="prepare-setup.xml" target="setup.platform">
                            <property name="setup.platform" value="${setup.platform}" />
                            <property name="setup.version" value="${project.version}" />
                            <property name="settings.localRepository" value="${settings.localRepository}" />
                        </ant>
                    </target>
                </configuration>
                <executions>
                    <execution>
                        <id>do-setup</id>
                        <phase>install</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <!-- Liefert alle benötigten jars für die engine aus dem Artefakt jar und deren Abhängige -->
            <groupId>${project.groupId}</groupId>
            <artifactId>jar</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <!-- Plattformspezifischer C++ Kern -->
            <groupId>${project.groupId}</groupId>
            <artifactId>kernel-cpp</artifactId>
            <version>${project.version}</version>
            <classifier>${setup.platform}</classifier>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-nop</artifactId>
        </dependency>

        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>jetty</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
            <exclusions>
                <exclusion>
                    <groupId>${project.groupId}</groupId>
                    <artifactId>kernel-cpp-${osArch}</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>
</project>
