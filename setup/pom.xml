<?xml version="1.0" encoding="utf-8"?>
<!--
call prepare-setup.sh <version> to use this pom
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <groupId>com.sos.scheduler.engine</groupId>
        <artifactId>engine</artifactId>
        <version>1.3.12.2132-SNAPSHOT</version>
    </parent>
    
    <properties>
      <setup.version>${project.version}</setup.version>
      <setup.platform>${osArch}</setup.platform>
    </properties>
    
    <artifactId>setup</artifactId>
    <packaging>pom</packaging>
    <name>${project.artifactId}</name>

    <profiles>
    
      <!-- this profile is for preparing the setup for a given version on a given platform, e.g.
            mvn install -Pprepare -Dversion=1.3.11 -Dplatform=windows-x86
      -->
      <profile>
        <id>prepare</id>
        <properties>
          <setup.version>${engine.version}</setup.version>
          <setup.platform>${engine.platform}</setup.platform>
        </properties>
      </profile>
      <profile>
        <id>prepare-snapshot</id>
        <properties>
          <setup.platform>${engine.platform}</setup.platform>
        </properties>
      </profile>
    </profiles>

    <build>
      <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
                <execution>
                    <id>kernel</id>
                    <phase>install</phase>
                    <goals>
                        <goal>copy</goal>
                        <goal>copy-dependencies</goal>
                    </goals>
                    <configuration>
                        <artifactItems>
                            <artifactItem>
                                <groupId>${project.groupId}</groupId>
                                <artifactId>jar</artifactId>
                                <version>${setup.version}</version>
                                <type>jar</type>
                            </artifactItem>
                            <artifactItem>
                                <groupId>${project.groupId}</groupId>
                                <artifactId>jar</artifactId>
                                <version>${setup.version}</version>
                                <type>pom</type>
                            </artifactItem>
                            <artifactItem>
                                <groupId>${project.groupId}</groupId>
                                <artifactId>kernel-cpp-${setup.platform}</artifactId>
                                <version>${setup.version}</version>
                                <!-- classifier>${setup.platform}</classifier -->
                                <type>jar</type>
                            </artifactItem>
                        </artifactItems>
                    </configuration>
                </execution>
            </executions>
        </plugin>
        <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <configuration>
                <target>
                    <ant antfile="prepare-setup.xml" target="setup.platform">
                       <property name="setup.platform" value="${setup.platform}" />
                       <property name="setup.version" value="${setup.version}" />
                       <property name="settings.localRepository" value="${settings.localRepository}" />
                    </ant>
                </target>
            </configuration>
            <executions>
                <execution>
                    <phase>install</phase>
                    <goals>
                        <goal>run</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
      </plugins>
    </build>

    <dependencies>
        
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>kernel</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>
        
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>cppjava.runtime</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>
        
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>jetty</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>
        
    </dependencies>
</project>
