<?xml version="1.0" encoding="ISO-8859-1"?>
<!--project default="build" basedir=".">

  <property name="cpp.dir.prod" value="../core-cpp/prod"/>

	<target name="build" depends="jar2devel">
		<echo>build is done!</echo>
	</target>

	<target name="jar2devel" description="copy the jar file into the development environment.">
    <copy file="target/${project.name}" todir="${cpp.dir.prod}/bin" />
    <copy file="target/${project.name}" todir="${cpp.dir.prod}/bind" />
	</target>

</project-->