package com.sos.scheduler.engine.data.scheduler;

import com.sos.scheduler.engine.data.base.StringValue;

public class SchedulerId extends StringValue {
    public SchedulerId(String x) {
        super(x);
    }
}
