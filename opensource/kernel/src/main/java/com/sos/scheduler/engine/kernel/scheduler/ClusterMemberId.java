package com.sos.scheduler.engine.kernel.scheduler;

import com.sos.scheduler.engine.kernel.util.*;

public class ClusterMemberId extends StringValue {
    public ClusterMemberId(String x) {
        super(x);
    }
}
